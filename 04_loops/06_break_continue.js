// Break & Continue

for (let index = 1; index <= 20; index++) {

    // continue: ara verir
    if (index === 10) {
        continue;
    }

    // break: döngüyü kırar
    if (index === 15) {
        break;
    }
    console.log(index);
}
